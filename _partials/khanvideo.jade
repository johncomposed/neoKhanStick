//- Functions ( Need to be moved elsewhere)
- var datadr = public[current.path.slice(0,1)]._data.content
- function cap(string) {
- return string.charAt(0).toUpperCase() + string.slice(1).replace(/-/g, " ");
- }

for obj in datadr
  each page, i in obj.pages
    if page.slug == current.source
      .row
        .col.c2
          br
        #content.col.c8
          h1= cap(page.slug)
          p#description
            != yield
          video.video-js.vjs-default-skin(width='100%', height='450', controls='', preload='auto', data-setup='{}', autoplay='')
            source(src='../media/#{ page.media }.mp4', type='video/mp4')
            track(kind='captions', src='../media/#{ page.media }.srt', srclang='en', label='English')
            | Your browser does not support video. Please get the latest version of
            | Google Chrome, Firefox, IE, or Safari. Alternately, you can try the link
            | below if you have VLC or another media player installed.
          p#direct
            | If the above player doesn't work, you can 
            a(href='../media/#{ page.media }.mp4') try this direct link.
          .prevnext
            != partial("prevnext")
          .nav
            != partial("breadcrumbs")
            != partial("meganav")
