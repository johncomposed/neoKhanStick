#content
//
  p
    | How is a differential equation different from a regular one?  Well, the solution is a function (or a class of functions), not a number.
    | How do you like me now (that is what the differential equation would say in response to your shock)!
  .playlist
    ul
        li
          a(href='/first-order-differential-equations/what-is-a-differential-equation.html', name='what-is-a-differential-equation')
            img(src='/media/-_POEWfygmU.jpg', alt='')
            b What is a differential equation
            p What a differential equation is and some terminology.


h1 Index Functionality 
p Goes through all the _data.json in subject-specific subdirs
mixin tree(head, tail)
  for val, key in head
    if (['_data'].indexOf(key) > -1)
      - var cols = val.children.length
      - var n = Math.floor(12/cols)
      .row
        h2 #{ val.title }
        p= val.description
        for obj in val.children
          .col(class=["c#{ n }"])
            h4= obj.title
            ul
              each page in obj.children
                li(class="#{ current.source == page.slug ? 'active' : '' }")
                  a(href=".#{ tail + obj.id + '/' + page.slug }.html") #{ page.title }
    if (['.git', '_data', 'assets', '_partials'].indexOf(key) == -1) && key != '_contents'
      mixin tree(val, tail + key + "/")

+tree(public, "/")
